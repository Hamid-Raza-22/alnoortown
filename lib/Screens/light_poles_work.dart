import 'package:flutter/material.dart';

class LightPolesWork extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Road Maintenance"),
      ),
      body: Center(
        child: Text("Road Maintenance Page Content"),
      ),
    );
  }
}
